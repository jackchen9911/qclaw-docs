---
title: 图像生成
description: 'Image Generation API'
icon: 'image'
---

# 图像生成

`POST /v1/images/generations`

## 可用模型

| 模型 | 功能 |
|------|------|
| `gpt-image-1` | OpenAI 文生图 / 图生图 |
| `flux-2-pro` | Flux 2 Pro 高质量文生图 |
| `flux-kontext` | Flux 图像编辑 |
| `imagen-4` | Google Imagen 4 |
| `ideogram-v3` | Ideogram V3 文生图 |

## 请求参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `model` | string | 是 | 模型名称 |
| `prompt` | string | 是 | 图像描述 |
| `n` | integer | 否 | 生成数量，默认 1 |
| `size` | string | 否 | 图像尺寸，如 `1024x1024` |
| `quality` | string | 否 | 图像质量，`standard` 或 `hd` |

## 请求示例

<CodeGroup>

```python Python
from openai import OpenAI

client = OpenAI(
    base_url="https://api.qclawrouter.com/v1",
    api_key="sk-your-api-key"
)

response = client.images.generate(
    model="gpt-image-1",
    prompt="一只在太空中漂浮的猫，赛博朋克风格",
    n=1,
    size="1024x1024"
)

print(response.data[0].url)
```

```bash cURL
curl https://api.qclawrouter.com/v1/images/generations \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-image-1",
    "prompt": "一只在太空中漂浮的猫",
    "n": 1,
    "size": "1024x1024"
  }'
```

</CodeGroup>

## 响应格式

```json
{
  "created": 1234567890,
  "data": [
    {
      "url": "https://..."
    }
  ]
}
```
